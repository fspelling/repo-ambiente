name: Deploy Fake
on:
  workflow_call:
    inputs:
      ambiente:
        type: string
        required: true
jobs:
  deploy-fake:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy fake no ambiente
        run: echo "Realizando deploy fake no ambiente ${{ vars.AMBIENTE_DEPLOY }}"

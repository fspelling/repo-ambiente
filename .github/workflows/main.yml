name: CD Fake
on:
  workflow_dispatch:
    inputs:
      ambiente:
        type: choice
        default: Hml
        options:
          - Hml
          - Prd
jobs:
  deploy-fake:
    runs-on: ubuntu-latest
    environment: Hml
    steps:
      - name: Deploy fake no ambiente
        run: echo "Realizando deploy fake no ambiente ${{ inputs.ambiente }}"
